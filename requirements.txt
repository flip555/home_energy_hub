# Core dependencies
homeassistant==2024.1.6  # Latest stable version
pymodbus==3.6.6  # For modbus communication

# Logging
colorlog==6.8.2  # For colored console output

# Development dependencies
pip>=21.0,<24.1  # Latest compatible version range
ruff==0.3.4  # Code linting

# Optional dependencies
# Add any optional dependencies here with proper version constraints

# Note: Version constraints use '==' for strict versions where stability is critical,
# and '>=' with upper bounds where flexibility is acceptable
